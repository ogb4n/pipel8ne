meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/api/auth/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}
