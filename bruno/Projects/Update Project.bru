meta {
  name: Update Project
  type: http
  seq: 6
}

patch {
  url: {{BASE_URL}}/api/projects/{{projectId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Updated Pipeline Name",
    "visibility": "public"
  }
}
